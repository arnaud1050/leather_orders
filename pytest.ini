[pytest]
testpaths = tests
# The project root has to be importable (app, models, communications) — there
# is no package install step, so pytest's rootdir insertion is what makes
# `import app` work from inside tests/.
pythonpath = .
filterwarnings =
    error::DeprecationWarning:communications.*
